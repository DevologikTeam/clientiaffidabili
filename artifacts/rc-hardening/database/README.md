# M21-S database evidence slot

Questa cartella deve ricevere evidenze reali di migrazioni, backup, restore drill e rollback.

La RC resta bloccata finche non esistono:

- migration baseline versionata;
- backup log;
- restore drill log su ambiente non produttivo;
- rollback plan approvato.
