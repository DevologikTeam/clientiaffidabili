# M21-S build evidence slot

Questa cartella deve ricevere i log reali del target prima della RC:

- `pnpm-build.log`
- `pnpm-typecheck.log`
- `docker-build.log`
- `healthcheck-summary.json`

I log devono essere redatti: nessun valore ENV, token, credenziale registry o payload cliente.
