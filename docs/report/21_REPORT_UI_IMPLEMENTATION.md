# Report UI Implementation

Componenti aggiunti:

- `ReportHero`
- `EvidenceCard`
- `ReportSection`
- `ReportActions`
- `ReportLimits`

La pagina `/reports/[id]` mostra una versione demo coerente con il modello dati, senza esporre dati reali o payload provider.

La pagina `/admin/reports` prepara la futura coda operativa interna.
