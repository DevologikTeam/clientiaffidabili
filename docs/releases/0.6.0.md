# Release 0.6.0 — M2-P Public Funnel Design

Data: 2026-05-29  
Sprint: **M2-P Public Funnel Design**  
Tipo: progettazione prodotto/UX/copy.

## Cosa cambia

- Trasformata l'analisi M2-A in blueprint operativo del funnel pubblico.
- Definiti wireframe testuali per homepage, catalogo servizi, dettaglio servizio e pagina prezzi.
- Definita architettura pagina-per-pagina per `/`, `/servizi`, `/servizi/[slug]`, `/prezzi`, `/api` e checkout entry.
- Definiti componenti funnel da sviluppare in M2-S: `ScenarioCard`, `TrustStrip`, `ReportPreview`, `HowItWorks`, `ComplianceNotice`, `PublicFAQ`, `PackageComparison`, `CheckoutEntryCard`.
- Creato copy MVP finale per hero, trust strip, scenari, pacchetti, come funziona e FAQ.
- Definita UI spec pubblica con regole desktop/mobile, report preview, pricing strip, footer ed error states.
- Definiti eventi analytics privacy-safe senza PII.
- Creato blueprint TypeScript contenutistico in `apps/web/lib/public-funnel/blueprint.ts` per accelerare lo sviluppo M2-S.
- Aggiunto QA script `qa:public-funnel-design`.
- Aggiornate versioni pacchetto a `0.6.0`.

## QA

Comandi eseguiti:

```bash
node scripts/qa-design-system.js
node scripts/qa-public-funnel-analysis.js
node scripts/qa-public-funnel-design.js
```

Esito: **passed**.

## Guardrail confermati

- Nessun claim assoluto di pagamento, solvibilità o rischio zero.
- Nessun linguaggio investigativo su persone fisiche.
- Prezzo, tempi, dati richiesti e limiti prima del checkout.
- Eventi analytics senza PII.
- Preview report senza dati reali.
- Uso professionale lecito dichiarato nel funnel.

## Prossimo sprint

**M2-S Public Funnel Development** — sviluppo effettivo delle pagine pubbliche e dei componenti funnel usando il design system esistente.
