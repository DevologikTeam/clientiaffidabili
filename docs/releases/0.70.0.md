# Release 0.70.0 — M20-P UX, SEO, Performance & Accessibility Polish Design

## Sprint completato

M20-P — UX, SEO, Performance & Accessibility Polish Design.

## Aggiunto

- Blueprint copy/trust per pagine pubbliche e checkout.
- Blueprint metadata, canonical, sitemap, robots e noindex per route pubbliche/sensibili.
- Blueprint navigazione mobile accessibile, skip link, landmark e focus state.
- Blueprint checkout guidato, pricing e pattern tabella/listini.
- Blueprint performance budget, Lighthouse, axe e osservabilita privacy-safe.
- Handoff M20-S con task P0/P1 e done criteria.
- Registry TypeScript `ux-seo-performance-a11y-design.ts`.
- QA script `qa-ux-seo-performance-accessibility-design.js`.

## Fix integrato nello sprint

Integrato il fix **#32 Docker web build / DataTable columns**:

- `DataTable` supporta sia colonne typed sia colonne legacy stringa.
- `caption` e' opzionale per retrocompatibilita con componenti esistenti.
- Le colonne legacy vengono normalizzate internamente con render sicuro per righe array.
- Rimosso il warning Autoprefixer sostituendo `align-items:end` con `align-items:flex-end`.
- Aggiunto QA `qa-m20p-docker-build-fix-32`.

## Decisione

M20-P resta uno sprint di design/polish, ma include il fix minimo necessario per non lasciare bloccato il build Docker web segnalato dal log reale. M20-S dovra implementare il resto del polish runtime e, in ambiente con dipendenze installate, rieseguire `pnpm --filter @clientiaffidabili/web build`.

## QA

- `node scripts/qa-ux-seo-performance-accessibility-analysis.js`
- `node scripts/qa-ux-seo-performance-accessibility-design.js`
- `node scripts/qa-m20p-docker-build-fix-32.js`
- `node scripts/qa-source-syntax-smoke.js`
- `node scripts/security-secret-scan.js`
- `zip -T clientiaffidabili_foundation_v0_70_0.zip`

## Note

Non sono stati eseguiti Lighthouse reale, axe reale, Playwright reale, Docker/Coolify reale o provider sandbox reali. Il fix `DataTable` e CSS e' stato verificato staticamente nel pacchetto.
