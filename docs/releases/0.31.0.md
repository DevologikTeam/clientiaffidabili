# Release 0.31.0 — M9-S Security, Compliance & Production Hardening Development

## Nuovi output

- `SecurityModule` NestJS.
- Guard RBAC e object-level authorization scaffold.
- Servizi redaction, webhook security, object authorization e production readiness.
- Endpoint interni per controls, production gate, redaction preview, webhook signature preview e object access preview.
- Security headers base in `main.ts`.
- Script `security-secret-scan.js` e `security-production-gate.js`.
- Pagina interna `/admin/security`.
- Runbook backup/restore/incident.
- QA gate produzione.

## Decisioni

- Produzione ancora bloccata finché build reale, test BOLA, webhook sandbox, secret scan e restore drill non sono verificati.
- I guard sono pronti per essere collegati a auth reale nel modulo M11.
- Nessuna esposizione raw payload in log/UI.
- Nessun provider call se i gate P0 non sono verdi.

## QA

`node scripts/qa-security-compliance-hardening-development.js` — passed.
