# Release 0.40.0 — M12-S API Partner & Reseller Portal Development

## Tipo release
Sprint di sviluppo.

## Aggiunto
- Runtime `PartnerPortalModule`.
- Entita' TypeORM partner, API key, webhook, usage ledger, rate limit, live request e idempotency.
- Controller partner portal, partner API `/api/partner/v1` e admin partners.
- Servizi API key hash, usage ledger, rate limit, webhook signing e sandbox.
- OpenAPI partner scaffold `apps/api/openapi/partner.v1.yaml`.
- Pagine partner `/dashboard/partner`, API key, docs, usage, webhook, go-live.
- Pagine admin `/admin/partners` e detail.
- QA antiregressione M12-S.

## Decisioni
- Sandbox-first confermato.
- Live disabilitato di default.
- Nessuna chiamata provider reale nel portale partner MVP.
- API key mai salvata in chiaro.
- Usage ledger append-only.

## QA
`node scripts/qa-api-partner-reseller-development.js` passed.

## Limiti
Scaffold offline. Build reale, test E2E, rate limit persistente, webhook delivery reale e production gate rimandati a M13.
